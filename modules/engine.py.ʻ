

import platform
import shutil

def get_sys_info():
    # Ambil info sisa storage
    total, used, free = shutil.disk_usage("/")
    # Ambil info arsitektur/model sistem
    model = platform.machine()
    return {
        "storage": f"{free // (2**30)} GB",
        "model": model
    }
